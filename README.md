# Old_code Branch

This branch contains earlier experiments and initial development code from the thesis project. It is kept for reference and historical context, not for running the robot.

## Repository Branches

| Branch | Description |
|--------|-------------|
| `fine-tuning` | Fine-tuning scripts and experiments (e.g. episode visualization) |
| `franka-gripper` | Franka Hand gripper, debugging mid-task stop (unresolved) |
| `robotiq-gripper` | Main benchmark branch with Robotiq gripper, VLM agent, and WhatsApp bot |
| `Old_code` | **This branch** — earlier experiments and initial development code |

## Notes

- This code is not intended to be run as-is.
- For the main benchmark setup and instructions, see the `robotiq-gripper` branch and its README.
- For fine-tuning related code, see the `fine-tuning` branch.
